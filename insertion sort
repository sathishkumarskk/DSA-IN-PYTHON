l=eval(input("enter: "))
for i in range(0,len(l)):
    if i!=0:
        for j in range(0,i):
            if l[j]>l[i]:
                t=l[j]
                l[j]=l[i]
                l[i]=t
print(l)                       
